export default function Interpolation(props)
{
    // alert(props.userName);
    // alert(props.userDesignation);
    return <h1> Hello {props.userName} designated as {props.userDesignation} from other component.</h1>
}